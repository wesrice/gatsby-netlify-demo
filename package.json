{
  "name": "gatsby-netlify-demo",
  "private": true,
  "scripts": {
    "clean:node_modules": "find . -name \"node_modules\" -exec rm -rf '{}' +",
    "install:cms": "yarn --cwd cms install && yarn install:static-site-generator",
    "install:infrastructure": "yarn --cwd infrastructure install",
    "install:static-site-generator": "yarn --cwd static-site-generator"
  },
  "version": "0.1.0",
  "dependencies": {}
}
